{
  "buildCommand": "NODE_ENV=production npm run build",
  "installCommand": "npm install --legacy-peer-deps --force --omit=dev",
  "env": {
    "NODE_ENV": "production",
    "NPM_CONFIG_LEGACY_PEER_DEPS": "true",
    "NPM_CONFIG_FORCE": "true"
  },
  "rewrites": [
    {
      "source": "/apps/(.*)",
      "destination": "/$1"
    }
  ]
}